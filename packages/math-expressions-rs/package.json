{
  "name": "math-expressions-rs",
  "version": "0.1.0",
  "description": "Rust port of math-expressions, compiled to WASM. This npm workspace member exists to build the wasm-bindgen output in pkg/; the crate itself is built with cargo.",
  "license": "(GPL-3.0 OR Apache-2.0)",
  "private": true,
  "scripts": {
    "build": "bash scripts/build-wasm.sh"
  }
}
