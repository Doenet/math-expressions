// Legacy `converters/flatten` — re-export of the tree flatten helpers.
import { flatten, unflattenLeft, unflattenRight, allChildren } from "../trees/flatten";

export { flatten, unflattenLeft, unflattenRight, allChildren };
export default flatten;
